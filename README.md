# HamAmooz_Task
This is a task for internship of Hamravesh (HamAmooz)


## How to Run
To run the program with the default settings (analyzing the file and displaying the top 10 endpoints), run exactly this command in your terminal:
`python CLI_Project.py access.log`

(Note: If needed, you can use the `python CLI_Project.py access.log --json` flag for JSON output,
and `--top 5` to change the number of items in the top endpoints list.)


## My important decision
Checking the input with REGEX (and also for better accuracy deleting spaces...)

## My challenge
thinking about some edge cases for input...
